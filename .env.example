# This is the environment configuration file for the VGU Care application. 
# This format is intended to contain sensitive information and should not be shared publicly.
# Please create an .env file in the backend folder and put in your own values.

# Below is only for reference.

# Database - PostgreSQL in Docker (Development)
DATABASE_URL=postgresql://vgu_user:vgu_password@postgres:5432/vgu_care
DB_HOST=postgres
DB_PORT=5432
DB_NAME=vgu_care
DB_USER=vgu_user
DB_PASSWORD=vgu_password

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_here

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000