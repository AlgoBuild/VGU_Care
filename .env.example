# This is the environment configuration file for the VGU Care application. 
# It contains sensitive information and should not be shared publicly.
# Please create an .env file in the backend folder and put in your own values.

# Below is only for reference.

# Database - PostgreSQL on AWS RDS
DATABASE_URL=postgresql://username:password@your-db-instance.region.rds.amazonaws.com:5432/vgu_care
DB_HOST=your-db-instance.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=vgu_care
DB_USER=your_username
DB_PASSWORD=your_password

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# AWS Configuration (if needed)
AWS_REGION=us-east-1